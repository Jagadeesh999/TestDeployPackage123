# ============================================================
# TestDeployPackage - Runtime Configuration
# ============================================================

# Application settings
app.name=TestDeployPackage
app.version=1.0.0
app.env=DEV

# Processing settings
processing.timeout.ms=30000
processing.retry.count=3
processing.retry.delay.ms=5000

# Logging
logging.level=INFO
logging.include.requestId=true